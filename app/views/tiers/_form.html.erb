<%= form_with(model: tier) do |form| %>
  <% if tier.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tier.errors.count, "error") %> prohibited this tier from being saved:</h2>

      <ul>
        <% tier.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :id, style: "display: block" %>
    <%= form.number_field :id %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :minSpent, style: "display: block" %>
    <%= form.number_field :minSpent %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
